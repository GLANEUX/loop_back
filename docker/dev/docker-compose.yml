services:
  api:
    build:
      context: ../..           # <-- revient à la racine (où se trouve package.json)
      dockerfile: docker/dev/Dockerfile
    container_name: loop_api_dev
    restart: unless-stopped
    env_file:
      - ../../.env.development
    ports:
      - "3000:3000"
    depends_on:
      - db
    volumes:
      - ../..:/usr/src/app     # monte le dossier racine dans le conteneur
      - /usr/src/app/node_modules
    networks:
      - loop_dev_network

  db:
    image: postgres:16-alpine
    container_name: loop_db_dev
    environment:
      POSTGRES_USER: loop_user
      POSTGRES_PASSWORD: loop_pass
      POSTGRES_DB: loop_dev
    ports:
      - "5432:5432"
    volumes:
      - pg_data_dev:/var/lib/postgresql/data
    networks:
      - loop_dev_network

networks:
  loop_dev_network:
    driver: bridge

volumes:
  pg_data_dev:
